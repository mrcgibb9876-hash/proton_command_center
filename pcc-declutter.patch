diff --git a/README.md b/README.md
index 3bcd0ee..5e95f7f 100644
--- a/README.md
+++ b/README.md
@@ -104,15 +104,27 @@ server-side. Skip them and everything else still works.
 ![Launch options built from toggles, with unsupported ones greyed out](assets/screenshots/launch.png)
 
 Compatibility tools are read from disk, so only builds you actually have are
-offered, and new releases show up on their own. Toggles cover DXVK, HDR,
-native Wayland, Reflex, esync, GE-only extras like D7VK and OptiScaler,
-and an Ultra+ mod loader toggle (`WINEDLLOVERRIDES=dwmapi=n,b`) for UE4SS-based
-mods from [theultraplace.com](https://theultraplace.com) - it forces Proton to
-load dwmapi.dll from disk instead of its own stub. Opening a game whose folder
-has a `ue4ss/` directory shows what Ultra+ found there (the loader DLL, any
+offered, and new releases show up on their own. The main toggle list sticks to
+what most people actually reach for - DXVK, HDR, native Wayland, MangoHud,
+Reflex, the NGX/DLSS auto-updater and auto-upgrade, VKD3D's experimental
+descriptor-heap path - plus an Ultra+ mod loader toggle
+(`WINEDLLOVERRIDES=dwmapi=n,b`) for UE4SS-based mods from
+[theultraplace.com](https://theultraplace.com) - it forces Proton to load
+dwmapi.dll from disk instead of its own stub. Opening a game whose folder has
+a `ue4ss/` directory shows what Ultra+ found there (the loader DLL, any
 companion `.asi` fixes); as of 1.22.0 those files are installed by Command
 Center itself, from the Mods tab.
 
+Niche, hardware-specific, or troubleshooting-only options - GameMode
+(`gamemoderun`), the 8GB-VRAM memory cap, D7VK, OptiScaler, the FSR4 upgrade
+(AMD-only), and disabling esync - aren't in that list; type them straight
+into "Extra env vars" if you need one (wrappers like `gamemoderun` belong
+there too, not in "Extra arguments" - that box runs after `%command%`, this
+one runs before it). Nothing already saved gets touched: a variable or
+wrapper word in a game's existing launch options that the toggle list no
+longer recognises round-trips through that same box instead of being
+silently dropped.
+
 A game with Ultra+ set up gets an **ULTRA+** badge on its library card - click
 the logo to jump to its Mods tab.
 
@@ -134,6 +146,20 @@ one-click restart.
 Every DLSS DLL in the game with its version. Swap in a newer one from your
 library, back up the original, roll back whenever. Requires an NVIDIA driver.
 
+The Launch tab's DLSS section covers the rest: Super Resolution mode and
+render preset, Frame Generation (2x-6x - 5x/6x are DLSS 4.5's Dynamic Multi
+Frame Generation, RTX 50-series only, and need a Proton/GE build with a
+recent-enough dxvk-nvapi), and Ray Reconstruction, all via the
+`DXVK_NVAPI_DRS_*` layer. Preset letters are read straight from the selected
+Proton build's own compiled dxvk-nvapi, so anything it doesn't actually
+support shows greyed out instead of silently doing nothing. "In-game overlay"
+is NVIDIA's own on-screen indicator - it overlays the live DLSS SDK version,
+resolution, and active mode right in the game (not something PCC draws
+itself), a one-checkbox way to confirm what's actually running without
+alt-tabbing out. Compact-vs-per-variable output and the save/apply-default
+template live under "Advanced preset options" so a first-time user isn't
+staring at raw NGX/DRS variable names before they've touched anything else.
+
 ### ReShade tab
 
 Scans the game's own exe for its graphics API (reading the PE import table -
diff --git a/index.html b/index.html
index 749279a..08b898b 100644
--- a/index.html
+++ b/index.html
@@ -495,9 +495,13 @@ textarea{width:100%;min-height:84px;background:#0c1014;border:1px solid var(--li
           <option value="1">Force 2x</option>
           <option value="2">Force 3x</option>
           <option value="3">Force 4x</option>
+          <option value="4">Force 5x (DLSS 4.5, RTX 50-series)</option>
+          <option value="5">Force 6x (DLSS 4.5, RTX 50-series)</option>
         </select>
       </div>
-      <p class="note">Multi-frame (3x/4x) is RTX 50-series; your 5070 qualifies. Pair with Reflex.</p>
+      <p class="note">Multi-frame (3x-6x) needs an RTX 50-series card. 5x/6x are new in DLSS 4.5's Dynamic
+        Multi Frame Generation and need a recent Proton/GE build (dxvk-nvapi 0.9.2+) - if your build's older,
+        pick 4x instead. Pair with Reflex.</p>
     </div>
     <div class="section">
       <h3>DLSS Ray Reconstruction</h3>
@@ -509,27 +513,35 @@ textarea{width:100%;min-height:84px;background:#0c1014;border:1px solid var(--li
       </div>
     </div>
     <div class="section">
-      <h3>DLSS preset options</h3>
-      <p class="note">Picking a render preset only tells the game which model to use — it doesn't fetch the model
-        itself. Turn on "DLSS auto-updater (NGX)" in the toggle list above too, or the preset may not exist yet
-        in this game's bundled DLSS DLL.</p>
+      <h3>In-game overlay</h3>
       <label class="row" style="align-items:center;gap:8px">
-        <input type="checkbox" id="drsCompact" checked>
-        <span class="note" style="margin:0">Emit as one compact <code>DXVK_NVAPI_DRS_SETTINGS=...</code> line (default). Unchecked emits one
-          <code>DXVK_NVAPI_DRS_&lt;KEY&gt;=</code> variable per setting instead — same effect, easier to read/edit by hand.</span>
-      </label>
-      <label class="row" style="align-items:center;gap:8px;margin-top:8px">
         <input type="checkbox" id="dlssDebugIndicator">
-        <span class="note" style="margin:0">On-screen DLSS indicator (debug) — DXVK_NVAPI_SET_NGX_DEBUG_OPTIONS. NGX remembers this setting in the
-          game's own prefix once it's launched with it on, so turning this off writes an explicit "off" line rather
-          than just deleting the variable — launch the game once more after unchecking to actually clear the overlay.</span>
+        <span class="note" style="margin:0">Show DLSS version in-game — overlays the live DLSS SDK version, resolution, and active mode
+          right on screen while you play. This is NVIDIA's own indicator (DXVK_NVAPI_SET_NGX_DEBUG_OPTIONS), not
+          something PCC draws itself, so it only appears in games the DLSS DLL actually supports it in. NGX
+          remembers this setting inside the game's own prefix once launched with it on, so turning it back off
+          writes an explicit "off" line rather than just deleting the variable — launch the game once more after
+          unchecking to actually clear the overlay.</span>
       </label>
-      <div class="row" style="margin-top:10px">
-        <button class="btn" id="dlssSaveDefault">Save as global default</button>
-        <button class="btn" id="dlssApplyDefault">Apply global default</button>
-      </div>
-      <p class="note">The global default is a template only — saving it never touches any game's launch options by
-        itself. "Apply" copies it into the SR/RR/FG controls above for the game you're currently editing.</p>
+    </div>
+    <div class="section">
+      <details>
+        <summary style="cursor:pointer"><h3 style="display:inline">Advanced preset options</h3></summary>
+        <p class="note">Picking a render preset only tells the game which model to use — it doesn't fetch the model
+          itself. Turn on "DLSS auto-updater (NGX)" in the toggle list above too, or the preset may not exist yet
+          in this game's bundled DLSS DLL.</p>
+        <label class="row" style="align-items:center;gap:8px">
+          <input type="checkbox" id="drsCompact" checked>
+          <span class="note" style="margin:0">Emit as one compact <code>DXVK_NVAPI_DRS_SETTINGS=...</code> line (default). Unchecked emits one
+            <code>DXVK_NVAPI_DRS_&lt;KEY&gt;=</code> variable per setting instead — same effect, easier to read/edit by hand.</span>
+        </label>
+        <div class="row" style="margin-top:10px">
+          <button class="btn" id="dlssSaveDefault">Save as global default</button>
+          <button class="btn" id="dlssApplyDefault">Apply global default</button>
+        </div>
+        <p class="note">The global default is a template only — saving it never touches any game's launch options by
+          itself. "Apply" copies it into the SR/RR/FG controls above for the game you're currently editing.</p>
+      </details>
     </div>
     <div class="section">
       <h3>Frame rate cap</h3>
@@ -769,20 +781,26 @@ const TOGGLES=[
  {id:"wayland",needs:"PROTON_ENABLE_WAYLAND",label:"Native Wayland",hint:"PROTON_ENABLE_WAYLAND=1 — better latency/pacing and HDR without Gamescope, but breaks the Steam Overlay",env:"PROTON_ENABLE_WAYLAND=1"},
  {id:"hdr",needs:"PROTON_ENABLE_HDR",label:"HDR",hint:"PROTON_ENABLE_HDR=1 DXVK_HDR=1 — disable the in-game HDR toggle if it conflicts",env:"PROTON_ENABLE_HDR=1 DXVK_HDR=1"},
  {id:"mangohud",label:"MangoHud overlay",hint:"Adds the mangohud wrapper — first enable writes PCC's custom horizontal overlay (orange labels, frametime graph, dGPU-pinned)",wrap:"mangohud"},
- {id:"gamemode",label:"GameMode",hint:"Feral GameMode: requests a temporary performance profile (CPU governor, I/O priority, GPU perf mode) for the duration of the game",wrap:"gamemoderun"},
- {id:"vram8g",label:"Optimise for 8GB VRAM",hint:"DXVK_CONFIG=\"dxgi.maxDeviceMemory=7168\" — caps the VRAM DXVK reports to the game at 7GB instead of the card's full 8GB. Leaving that headroom stops texture streaming from filling every last MB and thrashing against the desktop/compositor, which is what causes stutter on 8GB cards (3050/3060/4060/5060-class) in VRAM-heavy titles. Leave this off on cards with more than 8GB — it would just throttle memory you actually have.",env:"DXVK_CONFIG=dxgi.maxDeviceMemory=7168"},
  {id:"steamdeckon",label:"Steam Deck spoof (=1)",hint:"SteamDeck=1 — spoofs Deck mode; helps some anti-cheat (ACE) accept Linux",env:"SteamDeck=1"},
  {id:"steamdeckoff",label:"Force Steam Deck off (=0)",hint:"SteamDeck=0 — explicitly disables Deck mode when something else enabled it",env:"SteamDeck=0"},
  {id:"nvapireflex",label:"Reflex low-latency",hint:"DXVK_NVAPI_VKREFLEX=1 — exposes dxvk-nvapi's Vulkan Reflex layer. Only kicks in on games implementing VK_NV_low_latency2; helps latency/frame pacing there, harmless elsewhere. (PROTON_ENABLE_NVAPI was dropped: NVAPI has been on by default since Proton 9 and the variable exists in no current build.)",env:"DXVK_NVAPI_VKREFLEX=1",needs:"DXVK_NVAPI_VKREFLEX"},
- {id:"d7vk",needs:"PROTON_USE_D7VK",label:"D7VK (Direct3D 7)",hint:"PROTON_USE_D7VK=1 — routes ancient Direct3D 7 titles through DXVK. Off by default; GE enables it automatically for a couple of known games. Only useful on genuinely old games.",env:"PROTON_USE_D7VK=1"},
- {id:"optiscaler",needs:"PROTON_USE_OPTISCALER",label:"OptiScaler",hint:"PROTON_USE_OPTISCALER=1 — lets OptiScaler hook the game's upscaler so you can swap DLSS/FSR/XeSS at runtime. Needs OptiScaler itself installed in the game folder; the variable alone does nothing.",env:"PROTON_USE_OPTISCALER=1"},
  {id:"dlssupgrade",needs:"PROTON_DLSS_UPGRADE",label:"DLSS auto-upgrade",hint:"PROTON_DLSS_UPGRADE=1 — swaps the game's bundled DLSS DLL for the newest one Proton ships, without touching game files. Overlaps with the DLSS tab's manual swaps; pick one approach.",env:"PROTON_DLSS_UPGRADE=1"},
- {id:"fsr4upgrade",needs:"PROTON_FSR4_UPGRADE",label:"FSR4 upgrade",hint:"PROTON_FSR4_UPGRADE=1 — upgrades FSR 3.1 DLLs to FSR 4. RDNA4 only in practice; on your NVIDIA GPU this won't do anything useful.",env:"PROTON_FSR4_UPGRADE=1"},
- {id:"noesync",needs:"PROTON_NO_ESYNC",label:"Disable esync",hint:"PROTON_NO_ESYNC=1 — turns off eventfd synchronisation. A troubleshooting switch: try it only if a game hangs or crashes on launch, since esync is normally a performance win.",env:"PROTON_NO_ESYNC=1"},
  {id:"vkd3dheap",label:"VKD3D descriptor heap (experimental)",hint:"VKD3D_CONFIG=descriptor_heap — enables vkd3d-proton's newer Vulkan descriptor-heap path for D3D12 titles, meant to eventually replace the current descriptor-buffer implementation. Still opt-in upstream because of unresolved NVIDIA driver bugs (crashes/visual corruption reported in some titles, e.g. Ready or Not) — worth trying per-game, not as a default. (PROTON_VKD3D_HEAP=1 was the old opt-in for this and is being removed upstream; don't set it alongside this.)",env:"VKD3D_CONFIG=descriptor_heap"},
  {id:"ngxupdater",needs:"PROTON_ENABLE_NGX_UPDATER",label:"DLSS auto-updater (NGX)",hint:"PROTON_ENABLE_NGX_UPDATER=1 — the DLSS render-preset overrides below (DXVK_NVAPI_DRS_*) only pick which preset the game's own DLSS DLL should use; they don't fetch newer DLSS libraries. This lets Proton's own updater install newer DLSS DLLs so a picked preset (e.g. a recent transformer model) actually exists in the game to select. Turn this on alongside any render-preset override.",env:"PROTON_ENABLE_NGX_UPDATER=1"},
  {id:"ultraplus",label:"Ultra+ mod loader",hint:"WINEDLLOVERRIDES=dwmapi=n,b;version=n,b — two separate hijacks for two separate things. dwmapi.dll is the UE4SS build Ultra+ Manager drops in, loading UE4SS itself and UltraPlusExtensions. version.dll is a generic ASI loader (Ultimate ASI Loader) some games additionally need to run standalone .asi companion fixes (e.g. NaniteRayTracingFix.asi) that aren't part of UE4SS at all - harmless to leave on for games that don't have any. The status note below only appears once a game is open, and only means something if the files are already in the game folder — installed separately via Ultra+ Manager or, for the .asi fixes, wherever that fix is distributed.",env:"WINEDLLOVERRIDES=dwmapi=n,b;version=n,b"},
 ];
+// Trimmed from the default list (Aug 2026 declutter pass): GameMode
+// (wrap:"gamemoderun" — Feral GameMode; still fully supported, just type
+// "gamemoderun" into Extra env vars if you want it back), 8GB VRAM optimiser
+// (DXVK_CONFIG=dxgi.maxDeviceMemory=7168 — only matters on 8GB-class cards),
+// D7VK (PROTON_USE_D7VK=1 — only for a handful of ancient Direct3D 7 games),
+// OptiScaler (PROTON_USE_OPTISCALER=1 — needs OptiScaler installed in the
+// game folder to do anything, confusing as a bare toggle), FSR4 upgrade
+// (PROTON_FSR4_UPGRADE=1 — RDNA4-only, a no-op on NVIDIA), and Disable esync
+// (PROTON_NO_ESYNC=1 — a hang/crash troubleshooting switch, not an everyday
+// option). None of this breaks a game that already has one of these vars set:
+// loadStringIntoUI() below preserves anything it doesn't recognise into the
+// "Extra env vars" box instead of dropping it.
 let manualEdit=false;
 
 /* Which env vars each installed Proton build understands. Populated once at
@@ -1137,7 +1155,12 @@ function loadStringIntoUI(str){
   const leftover=beforeCmd.split(/\s+/).filter(Boolean).filter(tok=>{
     if(wrapWords.has(tok))return false;
     const eq=tok.indexOf("=");
-    if(eq<0)return false;              // not KEY=VALUE shaped - drop (shouldn't happen before %command%)
+    // Not KEY=VALUE shaped - a bare wrapper word (e.g. "gamemoderun" from a
+    // toggle that's since been trimmed off the main list, or one Steam/GE
+    // added on its own). Keep it verbatim rather than silently dropping it
+    // the next time any toggle rebuilds the string - a removed checkbox
+    // should never delete something a game already relies on.
+    if(eq<0)return true;
     const key=tok.slice(0,eq);
     return !managedKeys.has(key)&&!key.startsWith("DXVK_NVAPI_DRS_");
   });
